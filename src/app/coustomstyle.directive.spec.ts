import { CoustomstyleDirective } from './coustomstyle.directive';

describe('CoustomstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CoustomstyleDirective();
    expect(directive).toBeTruthy();
  });
});
